{erl_opts, [
    debug_info,
    {i, "include"}
]}.

{deps, []}.

{eunit_opts, [verbose]}.

{ct_opts, [{dir, "ct/testcases"}]}.

{cover_enabled, true}.

{plugins, [rebar3_hex]}.

{profiles, [
    {test, [
        {deps, []},
        {erl_opts, [debug_info, {i, "include"}]}
    ]}
]}.
